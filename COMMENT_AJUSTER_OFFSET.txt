"""
Script pour tester et ajuster l'offset global des pieds des méchants

┌─────────────────────────────────────────────────────────────────┐
│                  🎯 OFFSET GLOBAL SIMPLIFIÉ                     │
└─────────────────────────────────────────────────────────────────┘

Instructions :
1. Lancez le jeu normalement
2. Regardez si les pieds du méchant sont alignés avec ceux du personnage
3. Si c'est décalé, ajustez une SEULE valeur dans Donnees.py :

┌─────────────────────────────────────────────────────────────────┐
│  Ouvrez Donnees.py et cherchez MECHANT_FOOT_OFFSET              │
│                                                                  │
│  MECHANT_FOOT_OFFSET = 0  ← Ajustez cette valeur !              │
│                                                                  │
│  - Si les méchants sont TROP BAS : valeur NÉGATIVE (ex: -10)    │
│  - Si les méchants sont TROP HAUT : valeur POSITIVE (ex: +10)   │
└─────────────────────────────────────────────────────────────────┘

COMMENT ÇA MARCHE :

Le système actuel :
- position_y du personnage = bas du rectangle (rect.bottom)
- position_y du méchant = bas du rectangle (rect.bottom)
- MAIS les sprites peuvent avoir des espaces transparents différents en bas !

L'offset global compense ces espaces transparents :
- offset = 0 : pas d'ajustement (valeur par défaut)
- offset = -10 : monte TOUS les méchants de 10 pixels (pieds trop bas)
- offset = +10 : descend TOUS les méchants de 10 pixels (pieds trop hauts)

AVANTAGE DE L'OFFSET GLOBAL :
✓ Un seul paramètre à modifier pour tous les méchants
✓ Tous les sprites de méchants viennent du même pack d'assets
✓ Ils ont donc probablement tous la même structure d'espaces transparents
✓ Plus simple et plus rapide à ajuster !

EXEMPLE D'AJUSTEMENT :

Dans Donnees.py, cherchez cette section :

# ⚙️ ALIGNEMENT DES PIEDS DES MÉCHANTS
# Offset vertical global pour aligner les pieds visuels des méchants avec ceux du personnage
# - Si les méchants sont TROP BAS : utilisez un offset NÉGATIF (ex: -10)
# - Si les méchants sont TROP HAUT : utilisez un offset POSITIF (ex: +10)
# - 0 = pas d'ajustement
MECHANT_FOOT_OFFSET = -15  # ← Les méchants monteront de 15 pixels

Testez par petits incréments (±5 ou ±10) jusqu'à alignement parfait !

"""

print(__doc__)

